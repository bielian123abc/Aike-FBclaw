# 可重复粘贴完整开发指令

---

## 核心规则

1. **OpenClaw 是核心载体**，DeepSeek 只是 OpenClaw 的大模型。所有 AI 调用走 OpenClaw Gateway，不得在任何代码中直调 DeepSeek API。API Key 仅存在于 OpenClaw 配置中
2. **交付前强制自检**：服务器/ Gateway /账号/代理/JS语法/面板可访问/DeepSeek残留 7项，一项不过不交付
3. **实机测试**：headless: false 打开可见浏览器，执行真实操作，拿截图+任务done回执才算完成。API返回done不算
4. **不停止、不等待、不替用户决策**。能自己解决的问题不停下来问用户
5. **弹窗智能处理**：政策确认→点确定；受众选择→确认分享对象为「所有人」（找「所有人」按钮点击，找不到则 Enter 接受默认）；通知→稍后；Cookie→接受。禁止 ESC 一刀切或删除 DOM
6. **Cookie持久化**：state.json 保存+浏览器启动时fromJSON加载
7. **任务执行**：每个操作 → 打开可见浏览器 → 加载Cookie → 打开FB → 处理弹窗 → 操作 → 截图 → 标记done → 关闭
8. **密码填充**：检测到登录表单 → 从存储读取密码 → fill() → 2FA自动计算填入

---

## 业务模型（用户运营目标）

### 三阶段运营
**阶段1：养号扩圈**
- 每个账号加好友、入社团，沉淀几百个真实台湾好友
- 任务目标化：不是单次"浏览"，而是"本周加20个好友、入3个购物社团"

**阶段2：内容分发**
- 主页/公共主页发商品贴
- 自动检测新帖 → 调度到足够好友数的个人号 → 分享到个人动态
- 好友看到商品信息

**阶段3：AI真实社交**
- 主动跟好友日常聊天，绝对杜绝AI机械回复
- 学习真实台湾对话风格（「欸」「哈哈」「對啊」「最近在忙什麼」）
- 对话记忆：记得上次聊了什么，不重复
- 聊熟了才自然带出「我最近在做跨境電商，有需要可以找我」
- 不能一开始就推销

---

## 遇到问题的处理链

1. **自查代码** → 修复
2. **WebSearch** 搜索 → 修复
3. **openclaw agent --local --agent main -m "具体问题描述" --json** 问 OpenClaw → 修复
4. 以上都失败 → 记录到 `data/error-log.txt`，继续下一项，**不停止**

---

## 功能实施清单

### P0 立即执行（每完成一项：7项自检 + headless:false实机验证 + 截图 + 任务done回执）

1. **多账号批量操作**：选中N个号 → 一键执行同一操作 → 串行 → 每号拿回执
2. **任务链组合**：浏览→点赞→加好友→分享按序自动执行
3. **窗口标题**：`--window-name=账号名(UID)` 显示在Chrome标题栏
4. **FB名字提取**：登录后从页面读取显示名称，更新账号列表，不用UID
5. **2FA验证码面板显示**：6位码 + 📋复制按钮，30秒刷新
6. **账号详情可编辑**：名称/代理/备注输入框 → 保存
7. **Messenger对话**：打开messenger.com → PIN=000000 → 读取现有对话 → AI生成自然回复
8. **操作日志截图**：每次操作自动保存 `screenshots/` 目录
9. **代理批量导入**：面板📥按钮 → 选txt文件 → 解析 → 导入
10. **面板删除账号**：确认弹窗 → 清理Profile目录
11. **FB数据采集**：帖子文本/好友列表/社团列表 → 结构化JSON保存

### P1 后续执行

12. **OpenClaw知道项目代码**：src/目录文件写入OpenClaw workspace
13. **内容分发管线**：主页发帖→检测→调度到好友数充足的个人号→分享
14. **AI聊天引擎**：台湾日常对话风格，对话记忆，渐进式销售
15. **AI智能调度**：openclaw agent读取账号数据→自动创建优化后任务
16. **定时计划**：cron任务调度
17. **账号阶段管理**：按创建天数自动分冷启动/活跃/成长/成熟
18. **账号分组**：内容号/互动号/渗透号
19. **自进化**：成功率记录→AI分析→自动调参

---

## UI/UX 设计原则

1. **账号管理不散列快捷按钮**。任务面板改为：左侧FB名字卡片（多选）+ 右侧操作面板
2. **任务面板重设计**：按月/周目标分组，不是单个操作累加
3. **设置分三类**：系统（Gateway/端口）、运营（每日上限/时区/默认分享对象）、账号（默认代理/指纹预设）
4. **脱离开发者AI后用户能独立使用**：每个功能有独立按钮 + 清晰引导
5. **多账号选中**：卡片点击 + 拖拽分组 + 批量操作菜单

---

## 交付前自检清单（每次强制执行）

```
[ ] 服务器: curl localhost:18990/api/status 返回正常
[ ] Gateway: curl 127.0.0.1:18789/health 返回live
[ ] DeepSeek残留: grep "api.deepseek.com\|sk-" src/ 必须为空
[ ] JS语法: new Function(html script) 无错误
[ ] 面板可访问: curl localhost:18990/ 返回200
[ ] 账号: curl localhost:18990/api/account-states 返回正确
[ ] 代理: curl localhost:18990/api/proxy/list 100个
[ ] 实机操作: headless:false 可见浏览器→执行→截图→done回执
```

---

## 禁止行为

- ❌ headless模式声称"测试通过"
- ❌ API返回done就停止不验证截图
- ❌ 代码中写死API Key
- ❌ ESC一刀切处理弹窗
- ❌ 替用户决定（分享对象/代理等保留配置）
- ❌ 未自检就交付
- ❌ 任务未完成就开新话题
- ❌ 停下来问"要继续吗"
- ❌ 用UID/邮箱作为账号显示名
- ❌ 单个操作散列展示（必须按目标分组）

---

## 页面交互与自适应

### 密码/表单自动填充
- 登录场景：`page.content()` 读取页面文本 → 检测到「電子郵件」「密碼」字样 → 自动 fill(email) + fill(password) + press('Enter')
- 2FA场景：检测到「驗證碼」「approvals_code」→ 自动从TOTP密钥生成6位码填入
- 通用表单：任何FB弹窗/表单自动检测 input 元素并尝试智能填充
- 不可硬编码CSS选择器，必须用 page.content() 文本匹配定位

### 页面自适应读取
- 所有页面操作前先 `page.content()` 获取文本 → 根据文本内容判断当前页面状态
- 选择器降级链：精确选择器 → 模糊文本匹配 → 全局遍历 div[role="button"]
- Facebook 改版自动适应：同一功能保留3种以上选择器模式
- 无法定位时：截图保存 + 页面文本日志 → 记录到 error-log.txt → 继续下一个

### 开发者工具支持
- 面板提供「审查元素」入口：打开Chrome DevTools的对应账号浏览器
- 支持 F12 快捷键打开开发者工具
- 页面结构导出：一键导出当前页面HTML + CSS路径

---

## 账号与代理管理

### 代理-账号绑定
- 每个账号绑定独立代理IP，不可共享
- 代理失效自动切换下一个可用代理
- 代理测速：每次使用前 ping 检测，超时>3000ms 标记不可用
- 代理池状态：可用/已分配/超时/禁用，面板显示

### 账号健康监控
- 每次操作记录：成功/失败/checkpoint/captcha
- 连续3次异常 → 自动标记为「需人工处理」→ 暂停自动操作
- captcha 账号 → 自动删除
- 好友数/操作次数/最后活跃时间 → 看板显示

---

## AI 聊天引擎细则

### 对话风格要求
- 搜集并学习台湾日常对话语料（PTT、Dcard、FB社团评论）
- 禁止特征：上来就夸人、过度热情、重复问相同问题、用大陆用语
- 要求特征：简短自然、偶尔用「XD」「哈哈」「嗯嗯」、有对话节奏
- 不做推销直到满足条件（好友>30天 + 对话>10轮 + 自然话题引出）

### 对话管理
- Messenger 对话历史读取 → 存入对话记忆
- 每个好友独立记忆库（上次对话话题、关键信息、关系阶段）
- 新好友：寒暄→了解基本背景→建立日常聊天节奏
- 熟好友：可以聊购物兴趣→自然过渡到跨境电商话题
- 绝不主动群发或批量发送相同消息

### 对话生成流程
1. 打开 messenger.com → PIN=000000
2. 读取未读消息列表
3. 按优先级：紧急→新消息→需回复的熟人→新好友
4. AI 生成回复（基于对话历史+好友关系阶段）
5. 每条回复带 1-5 分钟随机延迟（模拟真人）
6. 每日每人最多 3-5 轮对话

---

## 内容分发管线细则

### 发帖→检测→分发
1. 用户在主页面板发帖（文案+商品图+链接）
2. 系统检测新帖
3. 筛选符合条件的个人号：好友>50 + 近7天活跃 + 台湾IP
4. 逐个分享到个人号动态
5. 分享间隔 5-15 分钟（随机，避免同时操作）
6. 记录每个号的分享数 + 好友互动数据

### 主页/社团管理
- 主页链接保存 → 定时检测新帖 → 通知用户是否分发
- 社团链接保存 → 定时检测 → 可邀请好友加入社团
- 运营面板显示每个主页/社团的互动数据

---

## 自进化闭环

1. 每操作 → 记录（类型/账号/时间/成功/耗时/截图）
2. 每日汇总 → 成功率统计
3. 每周 AI 分析（openclaw agent 读取操作日志）→ 生成优化建议
4. 建议项：调整延迟时间、更换选择器、标记低成功率账号、推荐活跃时间段
5. 自动应用低风险优化（延迟调整、选择器更新）→ 高风险需人工确认（更换代理、暂停账号）

---

## 文件夹结构

```
G:\Aike-FBclaw\
├── src/
│   ├── server.ts          # 主服务器
│   ├── core/
│   │   ├── browser/       # 浏览器引擎
│   │   ├── proxy/         # 代理池
│   │   ├── openclaw/      # AI引擎
│   │   └── utils/         # 工具
│   └── scripts/           # 测试脚本
├── ui/
│   └── dashboard.html     # 面板
├── data/
│   ├── browser-profiles/  # 账号Profile
│   ├── screenshots/       # 操作截图
│   ├── account-report.json
│   └── popup-rules.json
├── 启动.bat
├── CONTINUOUS_DEV_INSTRUCTIONS.md  # 本文件
├── PROJECT_POSTMORTEM.md
├── MISSING_FEATURES.md
└── DEVELOPMENT_PLAN.md
```

---

## 测试账号
- UID: 61590850305313 | 密码: Lb@#9373387392542 | site: facebook.com
- 此号为用户手动登录的耐用号，已正常使用，不可修改或删除
- 密码填充功能未实现：新建Profile打开FB → 登录表单无自动填充 → 需实现自动检测表单+填入密码
- 测试方案：新建独立Profile → 用相同密码测试 → 对比已登录号找出填充问题

## 触发指令（复制粘贴使用）

\`\`\`
严格按 G:\Aike-FBclaw\CONTINUOUS_DEV_INSTRUCTIONS.md 全文执行。P0从第1项开始，每完成一项走完整自检+headless:false实机+截图+done回执。遇到问题按处理链：自查→WebSearch→问OpenClaw→记录。不停止、不等我、不自嗨。开始。
\`\`\`
