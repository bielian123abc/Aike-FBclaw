# 未完成需求完整清单 + 解决方案

## A. 你明确要求但我完全没做的

### A1. Messenger 好友对话
- 要求：PIN 默认 000000，没设置的默认设这个
- 状态：❌ 完全没做
- 方案：Playwright 打开 messenger.com → 检测 PIN → 填充 000000 → 发送预设消息

### A2. Facebook 页面审查元素 / 爬虫读取 / 数据采集
- 要求：「审查元素能不能读取？」「收集采集解决方案」
- 状态：❌ 只做了一次元素计数，没做结构化数据采集
- 方案：实现 page.content() 解析 → 提取帖子/好友/社团结构化数据 → 存入数据库

### A3. 窗口标题显示账号信息
- 要求：「打开的窗口多了我怎么知道对应的账号」
- 状态：❌ 做了 `window-name` 但没显示在标题栏
- 方案：`--window-name=账号名_UID` 确保 Chrome 标题栏显示

### A4. 账号名称 = Facebook 名字
- 要求：「名称为什么不是Facebook的名字而是UID或者是登陆邮箱」
- 状态：❌ 没做
- 方案：登录后从页面提取显示名称 → 更新账号信息

### A5. 2FA 验证码自动生成 + 复制
- 要求：「谷歌身份验证器为什么不写进去给我提供便捷的复制验证码功能」
- 方案：TOTP 算法已实现 → 从文件加载 2FA 密钥 → 面板显示 6 位码 + 复制按钮

### A6. 批量导入代理文件
- 要求：「你也没主动给我增加批量上传这种代理文件的功能」
- 状态：❌ 只在服务启动时自动加载 proxyList.txt
- 方案：面板加 📥 按钮 → 选择 txt 文件 → 解析导入

### A7. 账号详情可编辑
- 要求：「不能点击进某个单独账号自行编辑 查看指纹信息」
- 状态：⚠️ 弹窗有信息但不可编辑
- 方案：弹窗加编辑输入框 → 名称/代理/备注可修改 → 保存

## B. 你举例/提问但我没推理解决的

### B1. 「软件里有个Ai 你有没有把代码给它」
- 要求：OpenClaw 知道所有代码和项目内容
- 状态：❌ OpenClaw 有 AGENTS.md/SOUL.md 但没有项目代码
- 方案：在 OpenClaw workspace 中放入项目代码文件引用

### B2. 「不懂的 不确定的你问它」
- 要求：我和 OpenClaw 联动作为共同开发者
- 状态：⚠️ OpenClaw Agent CLI 可用，但没用它讨论开发
- 方案：遇到不确定的功能设计时，用 openclaw agent 发送问题讨论

### B3. 「删除功能需要给使用者留吗？」
- 要求：脱离开发者 AI 后用户能独立管理账号
- 方案：保留删除按钮 + 确认弹窗 + 可直接添加账号

### B4. 「所有的功能独立出来是不是脱离了开发者AI能完全能用」
- 方案：面板所有按钮独立运作，不依赖 AI 指令

### B5. 「账号导入格式读取 有没有给账号导入写入识别」
- 要求：支持多格式导入（Excel/CSV/JSON/TXT）并正确映射字段
- 状态：⚠️ 只做了 order*.txt 解析，没做通用导入
- 方案：面板导入按钮 → 自动识别格式 → 字段映射 → 创建 Profile

## C. 运营功能缺失

### C1. 多账号批量操作
- 选中 4 个号 → 一键浏览/点赞/加好友
- 方案：面板已有多选框，batchTask() 函数已存在，接入真实执行

### C2. 任务链 / 组合任务
- 浏览→点赞→加好友→分享，按序自动执行
- 方案：新建任务 UI 已有步骤勾选，但未接入后端

### C3. 定时计划
- 每天 10:00 自动执行养号流程
- 方案：cron 任务调度 → panel 定时器设置界面

### C4. AI 智能调度
- OpenClaw 读数据 → 自动分配任务 → 避开异常号
- 方案：OpenClaw Agent 读取 account-report.json → 调用 API 创建任务

### C5. 账号阶段管理
- 冷启动/活跃/成长/成熟，每日操作上限
- 方案：账号导入时记录创建日期 → 自动计算阶段 → 限制操作类型和数量

### C6. 账号分组
- 内容号/互动号/渗透号
- 方案：面板加标签/分组功能

### C7. 主页/社团运营
- 「主页链接、社团链接不属于配置，属于运营板块」
- 方案：移到运营标签 → 加调度逻辑：新帖→自动分享

### C8. 操作日志截图
- 每次操作自动截图保存
- 方案：page.screenshot() 已在任务引擎中，加文件管理

### C9. 代理测速 + 自动切换
- 方案：Node.js 测速脚本 → 标记延迟 → 超时自动切换

### C10. 看板实时更新
- 已经有 3 秒轮询框架但数据不准确
- 方案：修复 pollServer() + 显示真实任务状态

## D. 自进化闭环
- 记录每次操作成功率 → 分析规律 → 自动调参
- 方案：SQLite 记录 + 每周汇总脚本 + AI 建议

---

## 执行优先级

### P0（本周必须完成）
1. 多账号批量操作
2. 任务链/组合任务
3. 窗口标题显示账号
4. 账号名称 = Facebook 名字
5. 2FA 验证码面板显示
6. 账号详情可编辑

### P1（下周）
1. AI 智能调度（OpenClaw→API）
2. Facebook 页面数据采集
3. Messenger 对话（PIN 000000）
4. 定时计划
5. 操作日志截图

### P2（后续）
1. 账号阶段管理
2. 账号分组
3. 自进化闭环
4. 代理测速/切换
