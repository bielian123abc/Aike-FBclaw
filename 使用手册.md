# Aike-FBclaw 使用手册

> Facebook 多账号 AI 智能运营系统 · 完整使用指南
> 版本：1.0.8 ｜ 适用对象：跨境电商运营人员
>
> **v1.0.8 修订说明**：新增「1.2 核心获客模式：六步运营链路」——完整说明本软件通过什么方式运营、后期如何获客，并把每一步的落地方式关联到对应章节（账号/代理/技能/编排/策略/AI），作为全书阅读与上手主线。
>
> **v1.0.7 修订说明**：应反馈修正两处错误——
> 1. **启动方式**：安装版为 Electron 桌面程序（内嵌指挥中心窗口），非浏览器加载 `http://localhost:18991`（v2.2）。
> 2. **按钮名称**：逐页与界面核对（账号卡片 3 按钮、代理池 4 按钮、任务编排 19 芯片 + 5 按钮、伺服器模型 6 按钮等），并新增「UI 芯片名 ↔ 后端任务类型」对照表（v7），标注 6 处不一致（界面执行会报「未知任務類型」的已知问题）。

---

## 目录

1. [软件概述](#1-软件概述)
2. [安装与启动](#2-安装与启动)
3. [首次配置（必读）](#3-首次配置必读)
4. [账号管理](#4-账号管理)
5. [代理池管理](#5-代理池管理)
6. [功能页面详解（13 个页面）](#6-功能页面详解13-个页面)
7. [任务类型全表（17 种）](#7-任务类型全表17-种)
8. [技能中心（19 个技能）](#8-技能中心19-个技能)
9. [AI 智能执行与 AI 对话](#9-ai-智能执行与-ai-对话)
10. [运营策略配置](#10-运营策略配置)
11. [安全限制与防风控](#11-安全限制与防风控)
12. [功能组合指南（典型工作流）](#12-功能组合指南典型工作流)
13. [数据目录结构](#13-数据目录结构)
14. [常见问题与排错](#14-常见问题与排错)

---

## 1. 软件概述

### 1.1 这是什么

Aike-FBclaw 是一套 **Facebook 多账号 AI 智能运营系统**。核心特点：

- **每个账号独立指纹环境**：自建 CDP Chromium 浏览器引擎，每个账号有独立的 Canvas/WebGL/AudioContext 指纹、独立浏览器档案、独立代理出口 IP，互不关联。
- **AI 当大脑**：内置 OpenClaw 智能体（搭载 DeepSeek 模型），能自由对话、智能规划每个账号的运营动作、自动接管聊天回复。AI 不是脚本路由壳，而是真正的决策大脑。
- **全拟人操作**：所有 Facebook 操作模拟真人行为——随机延迟、非线性滚动、概率性点赞、人类打字速度。
- **台湾本地化**：繁体中文界面、Asia/Taipei 时区、台式用语。

### 1.2 核心获客模式：六步运营链路

**一句话定位**：Aike-FBclaw 不是简单的自动化工具，而是一套用「多账号 + 拟人化社交 + 好友式信任」替代广告投放的获客系统——流量与客户关系像真人社交一样自然生长，后期以好友身份直接转化客户。

软件的全部功能（账号、代理、技能、任务编排、运营策略、AI 大脑）都围绕下面这条**六步获客链路**设计。**建议新手按此顺序阅读与上手本手册**：

| 阶段 | 目标 | 软件如何实现 | 对应章节 |
|------|------|-------------|---------|
| ① **矩阵搭建** | 批量准备账号与独立网络环境 | 批量导入账号 + 代理池上传，AI 自动为账号分配代理；每个账号独立浏览器指纹（Canvas/WebGL/AudioContext）、独立档案、独立出口 IP，互不关联，从浏览器层天然防风控防关联 | [第 4 章 账号管理](#4-账号管理)、[第 5 章 代理池管理](#5-代理池管理)、[3.2 配置代理池](#3-首次配置必读) |
| ② **养号冷启动** | 让账号先「活起来」，降低风控 | 首次登录自动完成繁中环境设置、自动更换头像；按 new→warmup→mature 三级安全限制逐步放开操作频率；AI 规划每日浏览动态、点赞、发布生活贴，模拟真人节奏养号 | [第 11 章 安全限制与防风控](#11-安全限制与防风控)、[第 8 章 技能中心](#8-技能中心19-个技能)、[第 9 章 AI 智能执行与 AI 对话](#9-ai-智能执行与-ai-对话) |
| ③ **好友圈扩张** | 建立第一层社交资产 | 自动加入兴趣社团/小组（`join_groups`）、从社团自然添加好友（`add_friends`）、自动打招呼（`greet_new_friends`），按真实人类频率控制每日上限（加好友/点赞/私信数量上下限） | [第 8 章 技能中心（社交扩张类）](#8-技能中心19-个技能)、[6.4 任务编排](#6-功能页面详解13-个页面)、[第 10 章 运营策略](#10-运营策略配置) |
| ④ **内容运营** | 主页长期沉淀优质内容 | 矩阵账号各自运营主页，按编排方案持续发布贴文（`create_post`）、分享贴文（`share_post`）、内容分发（`distribute_content`），让主页成为持续输出的获客阵地 | [第 8 章 技能中心（内容类）](#8-技能中心19-个技能)、[第 10 章 运营策略](#10-运营策略配置) |
| ⑤ **社交扩散** | 内容借好友圈裂变 | 矩阵账号根据各自当前社交圈，自动化分享主页内容——好友的好友看到分享 → 关注主页 → 继续扩散，形成一层层自然触达倍增 | [6.4 任务编排](#6-功能页面详解13-个页面)、[第 7 章 任务类型全表](#7-任务类型全表17-种) |
| ⑥ **关系深化与转化** | 把好友变成客户 | AI 自动与好友聊天（`ai_chat_reply` 被动接管），日常互动建立友谊与信任；运营后期在私信/主页中以好友身份自然转化客户，转化率与复购信任远超陌生广告触达 | [9.2 AI 对话](#9-ai-智能执行与-ai-对话)、[9.3 被动聊天接管](#9-ai-智能执行与-ai-对话)、[第 9 章 AI 智能执行](#9-ai-智能执行与-ai-对话) |

> 💡 **获客逻辑一句话**：先把人加进来（好友圈），再用内容养熟（主页 + 分享扩散），最后由 AI 陪聊建立信任，以好友身份成交。

**上手顺序建议**：新用户请严格按 ① → ⑥ 的顺序逐步搭建。
- **前两步（矩阵搭建 + 养号）是安全基础，务必先完成**——没有独立指纹/代理就批量操作，账号极易被 Facebook 关联封禁；新号直接高频互动同样高危。
- **③④⑤ 可并行**：好友圈扩张、内容运营、社交扩散可分别在多个账号上并行执行，或用 [6.4 任务编排](#6-功能页面详解13-个页面) 组合成管线批量跑。
- **⑥ 建议在账号有一定好友数后再开启 AI 接管**，避免新号高频聊天触发风控。

### 1.3 技术栈

| 层级 | 技术 |
|------|------|
| 桌面壳 | Electron + React + Vite |
| AI 引擎 | OpenClaw（内嵌）+ DeepSeek API |
| 浏览器 | 自建 CDP Chromium（随包附 428MB，无需另装） |
| 数据 | SQLite + JSON 文件记忆体 |
| 打包 | electron-builder → NSIS 安装包 |

### 1.4 架构分层

```
UI 指挥中心 (command-deck.html)
    ↓ HTTP API (端口 18991)
  ├── Commander 层 — 策略编排 + 异常处理
  ├── Squad 层 × N — 分组 + 频率控制
  └── Agent 层 × N — 浏览器控制 + 页面感知 + 技能执行
        ├── ProfileManager (CDP 启动 + 档案隔离)
        ├── FingerprintEngine (Canvas/WebGL/Audio 指纹)
        ├── FacebookPageDetector (28 页面 + 20 弹窗识别)
        ├── FbCoreSkills (10 个核心 FB 操作)
        ├── HumanBehavior (真人行为模拟)
        └── AccountMemory (SQLite 记忆体)
```

---

## 2. 安装与启动

### 2.1 安装

双击 `Aike-FBclaw Setup 1.0.6.exe`，按提示安装即可。安装包含：

- Electron 桌面程序主体
- 自带 Chromium 浏览器（428MB，无需另装 Chrome）
- 自带 Node.js 运行时（87MB，无需另装 Node）
- OpenClaw AI 引擎及其依赖

**升级须知**：直接覆盖安装即可。但**强烈建议安装前备份 `data/` 目录**（存放你的账号、代理、记忆等数据），以防意外丢失。

### 2.2 启动

**安装完成后，桌面会出现「Aike-FBclaw」快捷方式。双击它启动软件——启动的是 Electron 桌面程序，指挥中心界面直接内嵌在程序窗口中，不需要（也无需）再打开浏览器访问 `http://localhost:18991`。** 该地址是后端 API 地址，仅供调试使用。

> ⚠️ **重要**：安装版是桌面程序（Electron 内嵌指挥中心窗口），不是网页版。请从桌面快捷方式启动，不要在浏览器输入 `http://localhost:18991`。

启动后程序自动完成三件事：

1. **后台拉起 OpenClaw 网关**（端口 18789）—— AI 大脑的运行环境（窗口隐藏运行）
2. **后台拉起服务端 + Supervisor**（端口 18991）—— 负责浏览器控制、任务执行、API 服务（窗口隐藏运行）
3. **打开指挥中心主窗口** —— Electron 程序主窗口内加载指挥中心界面（全域监控页）

启动完成后你会看到指挥中心主界面（全域监控页）。

### 2.3 端口说明

| 端口 | 用途 |
|------|------|
| 18991 | 主服务端 API（API_PORT 默认值） |
| 18789 | OpenClaw 网关（AI 大脑） |
| 18992 | Supervisor 控制端口 + Mock Facebook 默认端口（两者共用默认值；真实账号模式不使用 Mock FB，不会冲突） |

---

## 3. 首次配置（必读）

新装软件后，**必须完成以下两步配置**才能正常使用 AI 功能和真实 Facebook 操作。

### 3.1 配置模型 API（第一步）

> **没有模型 API，AI 对话和 AI 智能执行都无法工作。** 这是硬性前提。

1. 进入 **「伺服器模型」** 页面（左侧导航第 09 项）
2. 填写三个字段：

| 字段 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| 基址 (baseUrl) | 模型 API 服务地址 | `https://api.deepseek.com` | `https://api.deepseek.com` |
| 金钥 (apiKey) | 你的 DeepSeek API Key | （空） | `sk-xxxxxxxxxxxxxxxx` |
| 模型 (model) | 模型名称 | `deepseek-chat` | `deepseek-chat` |

3. 点击 **💾 保存並熱生效** 按钮。系统会：
   - 将设置写入 `data/openclaw-config.json`
   - 自动套用到 OpenClaw 网关配置
   - **自动重启网关**（因为金钥通过环境变量 `DEEPSEEK_API_KEY` 注入网关，运行中的网关不会热更新 env，必须重启才生效）
4. 保存成功后会显示「已保存，已套用到网关」

**验证**：进入 **「AI 对话」** 页面，发一条消息，如果收到 AI 的真实回复（不是空响应），说明配置成功。

> **如果你的 Key 不是 DeepSeek**：目前软件的 AI 链路针对 DeepSeek 优化（`deepseek/deepseek-chat` 模型）。如需切换其他供应商，需修改网关配置，建议联系技术支持。

### 3.2 配置代理池（第二步）

> **没有代理，多账号会被 Facebook 识别为同一设备操作。** 每个账号必须有独立出口 IP。

1. 进入 **「代理池」** 页面（左侧导航第 02 项）
2. 添加代理：点击 **📎 上傳 TXT** 按钮，选择 `.txt`/`.csv` 文件批量导入（支持三种文本格式，详见 [第 5 节](#5-代理池管理)）。注意：**界面没有「手动添加代理表单」**，单条添加需通过 API `POST /api/proxy`
3. 在 **「代理池」** 页面点击 **🔗 自動分配**（或顶栏 **🔗 綁定代理**，两者等效），系统会按顺序为每个未绑定代理的账号分配一个空闲代理槽位

---

## 4. 账号管理

### 4.1 账号数据格式

账号数据存储在 `data/accounts.json`。每个账号包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `accountId` | string | ✅ | 账号唯一标识（通常用 FB 的 UID 数字） |
| `name` | string | ✅ | 账号备注名（登录后会自动同步为 FB 暱称） |
| `email` | string | ❌ | 登录邮箱（建议填写；xlsx 导入时 B 列为该字段） |
| `password` | string | ❌ | 登录密码（Cookie 失效时用于自动重新登录） |
| `twofa` | string | ❌ | 2FA 验证码（仅储存备用，暂不自动填入） |
| `stage` | enum | ✅ | 账号阶段：`new`（新号）/ `warmup`（养号中）/ `mature`（成熟号） |
| `mode` | enum | ❌ | 运行模式：`real`（真实 FB）/ `mock`（本地模拟）；缺省跟随全局配置 |
| `status` | enum | 自动 | 运行状态（见下表） |
| `proxy` | string | ❌ | 绑定的代理（系统自动管理） |
| `exitIp` | string | 自动 | 当前出口 IP（启动环境后检测） |
| `cookies` | string | ❌ | FB 会话 Cookie 字符串（用于免密登录） |
| `tags` | string[] | 自动 | 标签（如「含cookie」「含密碼」） |
| `notes` | string | ❌ | 备注 |
| `messengerPin` | string | 自动 | Messenger PIN（默认 000000） |
| `localeSetTw` | boolean | 自动 | 是否已设繁体中文界面 |
| `joinedGroups` | string[] | 自动 | 生涯已加入的社团 URL 列表 |
| `createdAt` | number | 自动 | 创建时间戳 |
| `lastUsed` | number | 自动 | 最后使用时间戳 |

### 4.2 账号状态含义

| 状态 | 图标 | 含义 | 处理方式 |
|------|------|------|----------|
| `offline` | ⚪ | 离线，未启动 | 点击「启动」开窗 |
| `idle` | 🟢 | 在线空闲 | 可执行任何任务 |
| `running` | 🔵 | 任务执行中 | 等待完成 |
| `error` | 🔴 | 出错（代理/登录失败等） | 检查代理、Cookie、密码 |
| `checkpoint` | 🟡 | 触发 FB 验证 | 需人工处理（身份/照片验证） |
| `needs_login` | 🟠 | 未登录且无法自动登录 | 补充密码或重新导入 Cookie |
| `dead` | ⚫ | 账号被封/禁用 | 已移出自动化，不再参与任务 |
| `deleted` | — | 已删除 | — |

### 4.3 账号导入（Excel 批量导入）

**这是添加账号的主要方式。** 准备一个 `.xlsx` 文件，格式如下：

| 列 | 字段 | 必填 | 说明 |
|----|------|------|------|
| A | UID / accountId | ✅ | FB 用户 ID（数字） |
| B | email / 登录账号 | ✅ | 登录邮箱 |
| C | Cookie 字符串 | ❌ | FB 会话 Cookie（datr/sb/xs/c_user/fr/pas…） |
| D | 登入密码 | ❌ | 登录密码（Cookie 失效时备用） |
| E | 2FA 验证码 | ❌ | 两步验证码（仅储存，暂不自动填入） |

**示例 xlsx 内容**：

| A (UID) | B (Email) | C (Cookie) | D (密码) | E (2FA) |
|---------|-----------|------------|----------|---------|
| 1000234567 | alice@mail.com | datr=xxx; sb=xxx; xs=xxx; c_user=1000234567; fr=xxx | MyPass123 | |
| 1000234568 | bob@mail.com | datr=yyy; xs=yyy; c_user=1000234568 | MyPass456 | |

**导入步骤**：
1. 点击**顶栏**的 **📎 上傳檔案** 按钮（支持 `.xlsx` / `.txt` / `.csv`；Excel 走完整导入流程）
   - 也可以点击 **📥 批量匯入**，在弹出框粘贴文本行（格式：`备注,登入标识,类型(real/mock)`，每行一条，例：`帳號A,61590344349141,real`）
2. 选择 `.xlsx` 文件（或粘贴文本后确定）
3. 系统自动完成：① 写入账号 ② 为每个账号生成独立指纹 ③ 还原会话 Cookie ④ 顺序分配代理
4. 导入结果会显示：总行数、成功创建数、跳过重复数、指纹生成数、Cookie 还原数、密码数、2FA 数、代理分配数

**导入规则**：
- 第一行如果是表头（含 uid/user/cookie/账号/密码 等关键词且非纯数字），自动跳过
- 已存在的 `accountId` 会跳过（不覆盖），计入「重复跳过」
- 导入的账号默认 `mode: real`、`stage: new`

### 4.4 单账号补充/修改

如果某个账号 Cookie 失效或缺少密码，不必整表重导入。**注意：界面没有「编辑」按钮**，修改账号资料需通过 API：

```
POST /api/account/{id}/update
{ "password": "...", "cookies": "datr=...; xs=...", "email": "...", "twofa": "...", "notes": "..." }
```

可修改：密码、Cookie、email、备注名、2FA。保存后 Cookie 会同步写入浏览器档案，下次启动即可恢复登录。

### 4.5 账号阶段（stage）

阶段决定每日操作上限（详见 [第 11 节](#11-安全限制与防风控)）：

| 阶段 | 说明 | 适用场景 |
|------|------|----------|
| `new` | 新号，最保守 | 刚导入、刚注册的账号 |
| `warmup` | 养号中，中等量 | 已稳定登录 3-7 天 |
| `mature` | 成熟号，放量 | 养号完成、行为稳定 |

**建议**：新号至少养号 7 天再升 `warmup`，`warmup` 维持 2-4 周再升 `mature`。

### 4.6 启动账号（开窗）

点击账号卡片上的 **▶ 啟動** 按钮后，系统自动执行**首登自愈管线**：

1. **检测登录状态**：有 Cookie → Cookie 直登；无 Cookie/失效 → 用账号密码登录
2. **同步 FB 暱称**：自动把 FB 上的昵称同步到账号备注（仅当备注还是数字 UID 时）
3. **设繁体中文**：把 FB 界面语言设为繁体中文（台湾）
4. **自动换头像**：从 `avatars/inbox/` 选一张未用头像上传替换（每个账号一生一次）
5. **Messenger PIN**：自动建立 PIN 码（默认 000000）
6. **主页点赞**：检测并点赞配置的种子粉丝页

以上每步都有记忆：二次登录检测到已完成即跳过，直到全部确认完成。

---

## 5. 代理池管理

### 5.1 代理数据格式

代理支持**三种文本格式**，可批量导入（每行一条）：

#### 格式 A：带协议头（推荐）

```
类型://用户名:密码@主机:端口
```

支持类型：`http`、`https`、`socks5`、`ssh`

**示例**：
```
socks5://user123:pass456@gw.dataimpulse.com:823
http://alice:secret@1.2.3.4:8080
https://user:pass@proxy.example.com:3128
```

#### 格式 B：无协议头（DataImpulse 经典格式）

```
用户名:密码@主机:端口
```

**说明**：用 `@` 作分隔，`@` 前为用户名:密码（首个冒号切分），`@` 后为主机:端口。
DataImpulse 的主机 `gw.dataimpulse.com` 会自动识别为 `socks5` 类型。

**示例**：
```
user123__cr.tw:pass456@gw.dataimpulse.com:823
alice:secret@1.2.3.4:8080
```

#### 格式 C：纯 IP:端口（旧式）

```
主机:端口
主机:端口:用户名:密码
```

**示例**：
```
1.2.3.4:8080
1.2.3.4:8080:alice:secret
```

### 5.2 代理数据字段

每个代理存储在 `data/proxies.json`，字段如下：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 代理 ID（系统生成，如 `pxy_1234567890_ab12`） |
| `name` | string | 代理名称（默认为原始文本） |
| `type` | enum | 类型：`http` / `https` / `socks5` / `ssh` |
| `host` | string | 主机地址 |
| `port` | number | 端口 |
| `username` | string | 用户名（可选） |
| `password` | string | 密码（可选） |
| `country` | string | 国家（可选） |
| `latency` | number | 连接延迟（毫秒，检测时刷新） |
| `lastCheck` | number | 最后检测时间戳 |
| `alive` | boolean | 是否可用 |
| `exitIp` | string | 真实出口 IP（穿透检测才有值） |
| `note` | string | 检测备注（正常为出口信息，异常为失败原因） |

### 5.3 代理检测（真实穿透检测）

本软件的代理检测**不是**简单的 TCP 端口握手，而是**真正通过代理发出 HTTPS 请求并验证出口 IP**：

- 通过代理访问 `https://api.ipify.org` 获取实际出口 IP
- 与直连 IP 比对：如果出口 IP 与直连相同，说明代理未真正转发（标记为不可用）
- 对 SOCKS5 代理（如 DataImpulse）：启动本地 SOCKS5 转发器 → Chromium 可用的本地 HTTP 代理 → 真实取出口 IP

**检测方式**：
- **批量检测**：点击代理池页面的 **⚡ 批量檢測** 按钮（同 host + 同用户名的代理视为一组，只检测一条代表，避免逐条 20 秒超时）
- **单条检测**：仅 API `POST /api/proxy/test/{id}`（界面无单条按钮）

### 5.4 代理分配

| 操作 | 说明 |
|------|------|
| **🔗 自動分配** | 在代理池页点击「🔗 自動分配」按钮（或顶栏「🔗 綁定代理」），系统按代理池顺序为每个未绑定代理的账号分配空闲槽位 |
| **分配（行内）** | 在代理表格「操作」列，对未绑定账号的代理点「分配」，手动选择目标账号 |
| **解綁（行内）** | 已绑定账号的代理显示「解綁」，点击释放该代理的账号绑定 |
| **删除** | 删除代理会同时解绑其关联的账号（界面无删除按钮，通过 API `DELETE /api/proxy/{id}`） |

**分配规则**：
- 已有绑定的账号保持不变
- 删除账号释放的槽位会被下次分配优先填回（轮空等待补充）
- SOCKS5 代理会启动本地转发器（`http://127.0.0.1:{port}`），Chromium 无需 SOCKS5 认证即可使用

### 5.5 Clash 接入（可选）

如果你使用 Clash 代理客户端，可在代理池页面配置 Clash API 地址和密钥，系统会自动拉取所有代理节点并支持测速、切换。

---

## 6. 功能页面详解（13 个页面）

左侧导航栏从上到下共 13 个功能页面（编号为页面角标）：

| 编号 | 页面 | 编号 | 页面 |
|------|------|------|------|
| 08 | 全域监控（默认首页） | 07 | 历史报告 |
| 01 | 账号环境 | 09 | 伺服器模型 |
| 02 | 代理池 | 10 | 社团总览 |
| 03 | 任务编排 | 11 | 头像管理 |
| 04 | 运营策略 | 12 | 技能中心 |
| 05 | 社团数据源 | 13 | 记忆体 |
| 06 | AI 对话 | | |

### 顶栏全局工具栏（所有页面可见）

| 按钮 | 说明 |
|------|------|
| 📥 批量匯入 | 弹出输入框，粘贴账号文本行（格式：`备注,登入标识,类型(real/mock)`）批量建号 |
| 📎 上傳檔案 | 选择 `.txt` / `.csv` / `.xlsx` / `.xls` 文件导入账号（xlsx 走完整导入流程） |
| 🔗 綁定代理 | 自动为未绑定代理的账号分配代理（等效「代理池」页的 🔗 自動分配） |
| 🔍 檢測 | 对全部账号执行 `sync` 状态检测 |
| 🤖 AI 執行 | 对全部已登录账号 AI 智能执行（见 [第 9 节](#9-ai-智能执行与-ai-对话)） |
| 🪟 重排 | 重排所有浏览器窗口 |
| 🔁 自動重排 | 切换自动重排开关 |
| ⏺ 示范錄制 | 对勾选账号（最多 6 个）开始录制真人操作示范 |
| ⏹ 示范停止 | 停止勾选账号的示范录制 |
| 📜 運行日誌 | 打开实时运行日志浮窗 |
| 👁 查看錄制 | 查看已录制的示范 |

### 6.1 全域监控（08）📊

**定位**：实时全局仪表盘，一眼掌握所有账号、代理、任务的运行状态。

**显示内容**：
- 生命体征卡：活躍帳號 / AI 接管中 / 任務吞吐 / 近期錯誤（各带趋势与迷你走势图）
- 中部图表：📈 任務吞吐波形（執行中/AI 編排/完成）+ 🖥️ 系統資源（CPU/RAM 环状仪表）
- 实时任务列表（monTasks）
- 实时事件流 / 会话信息（任务开始/完成/失败、账号状态变更、聊天消息等）

**操作**：本页为纯监控展示页，无操作按钮；全局操作在顶栏工具栏（见下）。

---

### 6.2 账号环境（01）👤

**定位**：账号的查看、筛选、启动/删除、状态管理，以及勾选账号供其他页面批量操作。

**页面结构**（从上到下）：
1. **搜索框**：`🔍 搜尋帳號 / 郵箱…` —— 输入即实时过滤
2. **筛选 chips**：`全部` / `運行中` / `空閒` / `異常` / `離線`（各带实时计数），另有 `已選 N` 显示当前勾选数
3. **账号卡片列表**：每张卡片含复选框、头像+状态灯、账号名+UID、状态徽章、「模式」「代理」两行信息

**账号卡片上的按钮（每张卡片只有 3 个）**：
| 按钮 | 说明 |
|------|------|
| ▶ 啟動 | 开窗并执行首登自愈管线（见 [4.6](#46-启动账号开窗)） |
| 🤖 智能執行 | 对该账号单独发起 AI 智能执行（规划并执行动作序列） |
| 🗑 刪除 | 删除账号（同时释放代理、清理指纹/档案/记忆/Cookie，需确认） |

> ⚠️ **注意**：界面**没有**「关闭 / 同步 / 截图 / 编辑」按钮。这些能力仅存在于后端 API：
> - 关闭窗口：`POST /api/account/{id}/close`
> - 同步状态：`POST /api/account/{id}/sync`
> - 截图：`POST /api/account/{id}/screenshot`
> - 修改账号：`POST /api/account/{id}/update`（见 [4.4](#44-单账号补充修改)）

**显示信息**：
- 账号备注名、UID（accountId）
- 当前状态徽章（颜色标识，见 [4.2](#42-账号状态含义)）
- 模式：真實登入（real） / 模擬（mock）
- 代理：出口 IP（绿色）或绑定代理地址；未绑定显示橙色「未綁定代理」

**勾选用途**：勾选账号后，可在其他页面联动操作——如「技能中心」对勾选账号执行技能、「任务编排」的「▶ 執行選中」对勾选账号执行编排、顶栏「示范錄制/示范停止」对勾选账号录制操作。

---

### 6.3 代理池（02）🌐

**定位**：代理的导入、检测、分配管理。详见 [第 5 节](#5-代理池管理)。

**页面按钮（只有 4 个）**：
| 按钮 | 说明 |
|------|------|
| 📎 上傳 TXT | 选择 `.txt` / `.csv` 文件批量导入（每行一条，支持三种格式，见 [5.1](#51-代理数据格式)） |
| ⚡ 批量檢測 | 批量真实穿透检测所有代理（同组只测一条代表） |
| 🔗 自動分配 | 按代理池顺序为未绑定代理的账号分配空闲槽位 |
| 📥 匯出映射 | 导出当前「代理 ↔ 账号」绑定关系 |

> ⚠️ **注意**：界面**没有**「手动添加代理表单」。单条添加代理需通过 API `POST /api/proxy`。

**表格列**：名稱 / Host:Port / 狀態 / 延遲 / 綁定帳號 / 操作
- 「操作」列：未绑定的代理显示「分配」按钮，已绑定的显示「解綁」按钮
- 空表时提示「尚無代理，點擊「上傳 TXT」匯入」

---

### 6.4 任务编排（03）⚡

**定位**：从「FB 功能庫」点选功能芯片（可多选），组成任务序列，对勾选账号执行。是手动触发自动化任务的核心入口。

**页面结构**（三块卡片）：

**① FB 功能庫（19 个功能芯片，按 4 类分组，可多选 toggle）**：

| 分类 | 芯片（data-task 名） |
|------|----------------------|
| 基礎 | 🔐 登入帳號(`login`)、🔄 同步狀態(`sync`)、👥 好友列表(`friends_list`) |
| 內容 | 📰 瀏覽動態(`browse_feed`)、👍 按讚貼文(`like_post`)、✍️ 發布貼文(`publish_post`)、📤 分享貼文(`share_post`) |
| 社交擴張 | 🔍 搜尋加好友(`search_add_friend`)、👤 按名稱加好友(`add_friend_by_name`)、💬 發送私訊(`send_message`)、📨 按名稱發私訊(`send_message_by_name`)、🏛️ 加入社團(`join_groups`)、🤝 邀請進社團(`invite_to_group`)、👍 邀請讚主頁(`invite_like_page`) |
| 智能編排 / AI 接管 / 安全 | 🤝 互加互聊(`socialize`)、👋 問候新好友(`greet_new_friends`)、📡 內容分發(`content_dispatch`)、🧠 AI 聊天接管(`ai_chat_reply`)、🛡️ 風控檢測(`risk_check`) |

默认选中 4 个：🔐 登入帳號、📰 瀏覽動態、📤 分享貼文、🤝 互加互聊。点击芯片即可选中/取消。

**② 任務管線（默认序列，4 节点）**：`login（登入帳號·驗證環境）→ browse_feed（瀏覽動態·隨機點讚）→ share_post（分享貼文·讀取數據源）→ socialize（互加互聊·智能編排）`，下方有提示：⚠️ 分享貼文依賴主頁/社團數據源，請確認已配置。

**③ 帳號分組**：A 組 · 蝦皮賣家 (5) / B 組 · 美妝團購 (4) / C 組 · 3C 數位 (3) / ＋ 新建分組（静态分组标签，可点选）。

**操作按钮（5 个）**：
| 按钮 | 说明 |
|------|------|
| 👁️ 預覽方案 | 预览当前编排（默认智能序列：sync → browse_feed → add_friends → join_groups → greet_new_friends → ai_chat_reply） |
| 💾 保存編排 | 把当前选中的芯片组合保存为本地编排（localStorage） |
| ▶ 執行選中 | 对**勾选的账号**执行当前选中的芯片序列 |
| ⚡ 全部執行 | 对**全部已登录账号**执行（服务端针对当前已登录账号规划） |
| ⏹ 全部終止 | 终止所有正在执行的任务 |

**使用方式**：
1. 在「账号环境」页勾选一个或多个账号（或点「⚡ 全部執行」对全部已登录账号执行）
2. 在 FB 功能庫点选要执行的功能芯片（可多选）
3. 点击 **▶ 執行選中**（对勾选账号）或 **⚡ 全部執行**
4. 执行结果记录到历史报告

**执行模式**：
- **单账号**：直接执行该任务
- **多账号**：逐个账号依次执行（`socialize` 类型除外，它自带动账号池，只执行一次）

> ⚠️ **已知问题**：FB 功能庫的芯片名（如 `publish_post`）与后端任务类型（如 `create_post`）部分不一致，直接执行个别芯片会报「未知任務類型」，详见 [第 7 节](#7-任务类型全表17-种) 的对照表。

**其他快捷入口**：
- 顶栏 **🤖 AI 執行**：对所有账号 AI 智能执行（见 [第 9 节](#9-ai-智能执行与-ai-对话)）
- 顶栏 **🪟 重排 / 🔁 自動重排**：重排浏览器窗口
- 顶栏 **⏺ 示范錄制 / ⏹ 示范停止**：对勾选账号录制真人操作示范（最多 6 个）
- 顶栏 **📜 運行日誌 / 👁 查看錄制**：查看运行日志与已录示范

---

### 6.5 运营策略（04）⚙️

**定位**：全局运营参数配置。详见 [第 10 节](#10-运营策略配置)。

**页面结构**：三列输入区（每日操作上限 / 真人行為延時 / 重複過濾）+ **💾 保存策略** 按钮。

**可配置项**：
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每日邀请上限 | 20 | 每个账号每天最多发出好友邀请数 |
| 每日分享上限 | 15 | 每个账号每天最多分享贴文数 |
| 每日点赞上限 | 50 | 每个账号每天最多点赞数 |
| 每日消息上限 | 20 | 每个账号每天最多发消息数 |
| 最小延迟(秒) | 2 | 操作间最小随机延迟 |
| 最大延迟(秒) | 8 | 操作间最大随机延迟 |
| 邀请去重 | 开启 | 同一对象不重复邀请 |
| 分享去重 | 开启 | 同一内容不重复分享 |

---

### 6.6 社团数据源（05）📡

**定位**：管理目标社团和公共主页的网址，供「加群」「分享」「邀请」类任务自动读取。

**页面结构**：
- **公共主頁** 卡片（邀請讚 / 分享來源）—— 数据源列表
- **台灣社團** 卡片（邀好友 / 加好友來源）—— 数据源列表
- **➕ 新增數據源** 表单：类型下拉（公共主頁 / 台灣社團）+ URL 输入框 + 名稱（可選）+ **添加** 按钮

**数据结构**：
| 字段 | 说明 |
|------|------|
| type | `group`（社团）或 `page`（公共主页） |
| url | 社团/主页网址 |
| name | 名称 |
| note | 备注 |
| rawId | 系统从 URL 提取的 ID |

**使用方式**：
1. 在「➕ 新增數據源」表单选择类型（公共主頁 / 台灣社團）
2. 填写网址（URL，例如 `https://www.facebook.com/groups/xxxx`）与名称（可選）
3. 点击 **添加** 按钮保存

**自动联动**：以下任务在未显式传入目标时，会自动从此处读取：
- `share_post`（分享贴文）→ 优先取第一个主页，否则取第一个社团
- `invite_to_group`（邀请进社团）→ 取第一个社团
- `add_friends_from_group`（从社团加好友）→ 取第一个社团
- `invite_to_page`（邀请赞主页）→ 取第一个主页

---

### 6.7 AI 对话（06）🧠

**定位**：与内置 OpenClaw AI 智能体自由对话。可以问任何问题、下达自然语言指令、让 AI 帮你规划运营。

**前提**：必须先在「伺服器模型」页配置 DeepSeek API Key。

**使用方式**：
- 在输入框输入消息，按回车发送
- AI 会以真实自然语言回复（不是罐头回复）
- AI 可以调用工具：读取账号状态、执行任务、规划运营等
- 回复中会显示 AI 调用了哪些工具（usedTools）和执行步骤（steps）

**对话示例**：
- 「帮我看看现在有几个账号在线」
- 「给所有在线账号安排今天的运营计划」
- 「alice 这个号最近表现怎么样」

---

### 6.8 历史报告（07）📋

**定位**：查看所有任务执行的历史记录。

**表格列**：任務 ID / 範圍 / 類型 / 時間 / 狀態

---

### 6.9 伺服器模型（09）🖥️

**定位**：AI 模型配置 + 系统状态诊断 + 一键修复。

**页面结构**：
- **服務端狀態** 卡片（監管啟動器）：显示服务端运行状态，按钮 **🔄 刷新 / ♻️ 重啟 / ⏹ 停止 / ▶ 啟動**
- **OpenClaw 模型設置** 卡片：API Key / 模型 / Base URL 三个输入框 + **💾 保存並熱生效** 按钮（见 [3.1](#31-配置模型-api第一步)）
- **一鍵修復** 卡片：**🔧 執行修復** 按钮

**状态诊断**：
- 网关是否可达（18789 端口）
- 配置是否可读
- 是否已配置模型
- 是否已配置金钥
- 服务端运行时长
- Supervisor 状态（是否在运行、是否在维护、重启次数）

**一键修复**：
- 如果 AI 对话无响应、网关配置损坏，点击 **🔧 執行修復**
- 修复流程：停網關 → 還原安全基線（last-good）→ 重注入模型 API → 重啟網關
- **不会影响账号数据**（accounts.json / 浏览器档案完全不动）

---

### 6.10 社团总览（10）👥

**定位**：跨账号查看社团加入情况，检测重复加群。

**显示内容**：
- 全局已加入社团列表（去重）
- 每个账号的加入数、重叠数
- 跨账号重叠社团（同一社团被多个账号加入）

**用途**：确保不同账号加入不同社团，避免关联。

---

### 6.11 头像管理（11）🖼️

**定位**：管理头像资源库，控制头像的分配与使用。

**规则**：
- 头像放在 `data/avatars/inbox/`（待用）
- 用过的头像自动移到 `data/avatars/used/` 并标记
- 每个账号一生只换一次头像
- 仅在 `warmup` 阶段自动套用
- 首次开号自动从 inbox 挑一张未用头像上传

**操作**：
| 操作 | 说明 |
|------|------|
| 📤 上傳頭像 | 从本机选图直接写入 inbox（支援 PNG/JPEG/WEBP/GIF/BMP，≤10MB） |
| 標記已用 | 手动标记某张头像已用（如你手动上传过） |
| 替換到賬號 | 在下拉框选择目标账号后，把该头像真正替换到 FB 账号（需账号已启动） |

---

### 6.12 技能中心（12）🎓

**定位**：查看和管理所有可调用的 AI 技能。详见 [第 8 节](#8-技能中心19-个技能)。

**显示内容**（表格列：技能 / 分類 / 說明 / 使用次 / 最後使用 / 狀態 / 操作）：
- 技能名称、类别、描述
- 使用次数、最后使用时间
- 启用/停用状态

**操作**：
- **執行**：对「账号环境」页勾选的账号，直接执行该技能（已停用的技能按钮置灰不可点）
- **停用 / 啟用**：切换技能启用状态。停用的技能不会被 AI 调用。

页面提示：「在『帳號環境』勾選帳號後，可對勾選帳號執行已啟用技能。」

---

### 6.13 记忆体（13）💾

**定位**：查看每个账号的记忆碎片和全局知识库。

**记忆碎片**：每次任务执行、聊天对话、重要事件都会写入记忆碎片，跨会话保留。

**全局知识库**：跨账号的经验笔记和已进化技能。

**查看方式**：
- 帳號記憶碎片表格：帳號 / 事實 / 重要 / 近期 / 關係 / 操作（「查看」按钮，点击查看该账号记忆详情）
- 全局知識庫：跨账号的经验笔记和已进化技能

**用途**：AI 智能执行和 AI 对话会读取记忆来做出差异化决策。

---

## 7. 任务类型全表（后端 17 种 ↔ 界面 19 芯片）

> ⚠️ **重要说明**：本表列出**后端 API 任务类型**（`/api/task/run` 用）与**「任务编排」页 FB 功能庫芯片名**的对应关系。两者**并非全部一致**：
> - 一致的（12 个）：`login`、`sync`、`browse_feed`、`like_post`、`share_post`、`add_friend_by_name`、`send_message`、`join_groups`、`invite_to_group`、`socialize`、`greet_new_friends`、`ai_chat_reply`、`risk_check`
> - **不一致的（6 个，界面点这些芯片直连后端会报「未知任務類型」）**：
>   - `publish_post`（界面）→ 后端应为 `create_post`
>   - `search_add_friend`（界面）→ 后端应为 `add_friends`
>   - `send_message_by_name`（界面）→ 后端应为 `send_message_to_name`
>   - `invite_like_page`（界面）→ 后端应为 `invite_to_page`
>   - `content_dispatch`（界面）→ 后端应为 `distribute_content`
>   - `friends_list`（界面）→ 后端应为 `get_friends`
> - **界面没有芯片、仅后端可用**（通过 API 调用）：`status_report`、`auto_like_own_page`、`add_friends_from_group`、`reply_comment`
>
> **建议**：日常操作优先使用界面芯片；如需上述 6 个不一致功能，请直接调用 API `/api/task/run` 并传**后端任务类型**（右列），或等后续版本修复界面映射。

### 7.1 基础任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `login` | ✅ 🔐 登入帳號 | 登入账号 | `email`, `password` | 自动登录 Facebook（未传则用账号储存的账密） |
| `sync` | ✅ 🔄 同步狀態 | 同步账号状态 | 无 | 检测页面状态、同步 FB 暱称、读取 Mock 状态 |
| `status_report` | —（仅 API） | 状态报告 | 无 | 纯读取型，统计账号/代理/会话/任务，不开浏览器 |

### 7.2 内容互动任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `browse_feed` | ✅ 📰 瀏覽動態 | 浏览动态 | 无 | 滑动首页、自然浏览贴文（拟人化滚动） |
| `like_post` | ✅ 👍 按讚貼文 | 按赞贴文 | 无 | 对帖子按赞（概率性随机，不全部点） |
| `create_post` | ⚠️ ✍️ 發布貼文（界面名 `publish_post`） | 发布贴文 | `content` | 发布生活/兴趣贴文 |
| `share_post` | ✅ 📤 分享貼文 | 分享贴文 | `postUrl`(可选), `target` | 分享主页/社团贴文到时间线；未传则自动从数据源读取 |
| `auto_like_own_page` | —（仅 API） | 自动点赞主页 | `ownPages`(可选) | 滚动首页，遇到自己主页的帖子概率性点赞（30-60%） |

### 7.3 社交扩张任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `add_friends` | ⚠️ 🔍 搜尋加好友（界面名 `search_add_friend`） | 搜索加好友 | `count`, `searchQuery`/`keyword` | 关键字搜索并送出好友邀请（受阶段上限限制） |
| `add_friend_by_name` | ✅ 👤 按名稱加好友 | 按名称加好友 | `name` | 指定 FB 名称送出好友邀请 |
| `add_friends_from_group` | —（仅 API） | 从社团加好友 | `groupId`(可选), `groupUrl`(可选) | 从目标社团成员中加好友；未传则自动从数据源读取 |
| `send_message` | ✅ 💬 發送私訊 | 发送私讯 | `profileUrl`, `friendName`, `message` | 对指定对象发私讯 |
| `send_message_to_name` | ⚠️ 📨 按名稱發私訊（界面名 `send_message_by_name`） | 按名称发私讯 | `name`, `message` | 指定 FB 名称发私讯 |
| `join_groups` | ✅ 🏛️ 加入社團 | 加入社团 | `count`, `groupUrls`/`groupUrl`/`keyword` | 加入目标社团（受阶段上限限制） |
| `invite_to_group` | ✅ 🤝 邀請進社團 | 邀请进社团 | `groupId`(可选) | 邀请好友加入目标社团；未传则自动从数据源读取 |
| `invite_to_page` | ⚠️ 👍 邀請讚主頁（界面名 `invite_like_page`） | 邀请赞主页 | `pageUrl`(可选), `count` | 邀请好友赞公共主页；未传则自动从数据源读取 |
| `get_friends` | ⚠️ 👥 好友列表（界面名 `friends_list`） | 取得好友列表 | 无 | 读取好友清单 |

### 7.4 智能编排任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `socialize` | ✅ 🤝 互加互聊 | 互加互聊 | `accountIds`(可选), `maxAdds`, `maxChats` | 账号池环状互加好友并互聊（AI 生成台式聊天），错开 20-60 秒 |
| `greet_new_friends` | ✅ 👋 問候新好友 | 问候新好友 | `count`(可选), `friends`(可选), `greetDelayMs`(可选) | 解析通知中「接受了你的好友请求」，进入对方主页浏览 20-60 秒，AI 生成个性化问候发送；默认 5-30 分钟错开 |
| `distribute_content` | ⚠️ 📡 內容分發（界面名 `content_dispatch`） | 内容分发 | `contentId`, `accountIds`, `staggerSeconds` | 多账号错峰分发内容（每个账号 AI 改写后发帖），默认错开 10 秒 |

### 7.5 AI 接管任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `ai_chat_reply` | ✅ 🧠 AI 聊天接管 | AI 聊天接管 | `peers`(可选) | 解析 Messenger 未读对话，AI 生成回复并模拟人类打字发送；含一级介绍检测；回完返回首页 |
| `reply_comment` | —（仅 API，未接入） | 贴文留言回复 | — | ⚠️ 尚未接入，会优雅返回状态（不开浏览器、不报错） |

### 7.6 安全检测任务

| 后端类型 | 界面芯片 | 名称 | 参数 | 说明 |
|----------|----------|------|------|------|
| `risk_check` | ✅ 🛡️ 風控檢測 | 风控检测 | 无 | 检测账号风控等级（low/medium/high/critical），基于页面状态和弹窗判断 |

---

## 8. 技能中心（19 个技能）

技能是 AI 可调用的能力单元。每个技能对应一个后端任务类型，可在「技能中心」页查看/执行/停用：

- 表格列：技能 / 分類 / 說明 / 使用次 / 最後使用 / 狀態 / 操作
- 「操作」列：**執行**（对「账号环境」页勾选的账号执行该技能，已停用则置灰）+ **停用/啟用** 开关
- 分类与「任务编排」页 FB 功能庫的分类一致（基础/内容/社交扩张/智能编排/AI 接管/安全），但**技能名采用后端任务类型**（见 [第 7 节](#7-任务类型全表17-种) 对照表）

### 基础（3 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| 登入账号 | `login` | 自动登入 Facebook |
| 同步账号状态 | `sync` | 同步 FB 暱称与页面状态 |
| 取得好友列表 | `get_friends` | 读取好友清单 |

### 内容（4 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| 浏览动态 | `browse_feed` | 滑动首页、自然浏览贴文 |
| 按赞贴文 | `like_post` | 对帖子按赞（拟人化随机） |
| 发布贴文 | `create_post` | 发布生活/兴趣贴文 |
| 分享贴文 | `share_post` | 分享主页/社团贴文到时线 |

### 社交扩张（7 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| 搜索加好友 | `add_friends` | 关键字搜寻并送出好友邀请 |
| 按名称加好友 | `add_friend_by_name` | 指定 FB 名称送出好友邀请 |
| 发送私讯 | `send_message` | 对指定对象发送私讯 |
| 按名称发私讯 | `send_message_to_name` | 指定 FB 名称发私讯 |
| 加入社团 | `join_groups` | 加入目标社团 |
| 邀请进社团 | `invite_to_group` | 邀请好友加入目标社团 |
| 邀请赞主页 | `invite_to_page` | 邀请好友赞公共主页 |

### 智能编排（3 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| 互加互聊 | `socialize` | 帐号池环状互加好友并互聊 |
| 问候新好友 | `greet_new_friends` | 对刚通过的好友自然问候 |
| 内容分发 | `distribute_content` | 多帐号错峰分发内容 |

### AI 接管（1 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| AI 聊天接管 | `ai_chat_reply` | 侦测未读并由 OpenClaw 接管回复（含一级介绍） |

### 安全（1 个）

| 技能 | 任务类型 | 说明 |
|------|----------|------|
| 风控检测 | `risk_check` | 检测帐号风控等级 |

---

## 9. AI 智能执行与 AI 对话

### 9.1 AI 智能执行

**入口**：
- 顶栏 **🤖 AI 執行** 按钮 —— 对**全部已登录账号**智能执行
- 账号卡片上的 **🤖 智能執行** 按钮 —— 对**该账号**单独智能执行
- API `/api/agent/smart-execute`（可选传 `accountIds`）

> 注：任务编排页本身没有「AI 智能执行」按钮，其 **⚡ 全部執行** 同样触发 AI 规划（服务端针对当前已登录账号规划后执行）。

**前提**：
1. 已在「伺服器模型」页配置 DeepSeek API Key
2. 至少有一个账号已启动并登录（有活跃浏览器会话）

**工作原理**：

```
1. 收集所有已登录账号
2. 为每个账号读取：名称、阶段、状态、记忆摘要（最近 10 条）
3. 把这些信息打包发给 OpenClaw（真 AI 大脑）
4. AI 返回 JSON 计划：每个账号本轮应执行的动作序列
5. 系统按计划执行
```

**AI 可选择的合法动作**（8 种）：

| 动作 | 说明 |
|------|------|
| `sync` | 同步状态 |
| `browse_feed` | 浏览动态 |
| `like_post` | 按赞贴文 |
| `add_friends` | 加好友 |
| `join_groups` | 加入社团 |
| `greet_new_friends` | 问候新好友 |
| `ai_chat_reply` | AI 聊天接管 |
| `post_content` | 发布贴文 |

**AI 规划原则**（由 prompt 约束）：
- 不给所有账号一样的动作，根据各自记忆/阶段差异化分配
- 新号少加好友、成熟号可多互动
- 每个账号 1~4 个动作，按合理先后顺序

**示例**：
- 账号 A（new，刚登录）→ `[sync, browse_feed, like_post]`（先熟悉环境）
- 账号 B（warmup，有新好友）→ `[greet_new_friends, browse_feed]`（问候 + 浏览）
- 账号 C（mature，有未读消息）→ `[ai_chat_reply, add_friends, like_post]`（回消息 + 扩张 + 互动）

### 9.2 AI 对话

**入口**：AI 对话页，或 API `/api/chat`。

**与智能执行的区别**：
- AI 对话是**自由对话**，你可以问任何问题、下达任何指令
- AI 智能执行是**一键自动规划**，AI 自主决定每个账号做什么

**AI 对话能力**：
- 读取账号状态、记忆
- 规划运营方案
- 解释软件功能
- 执行具体任务（通过工具调用）

**回复结构**：每次回复包含：
- `reply`：AI 的自然语言回复
- `steps`：执行步骤
- `usedTools`：调用了哪些工具

### 9.3 被动聊天接管

**入口**：API `/api/passive/start`（可设置扫描间隔，默认 90 秒）。

**工作方式**：
- 定期扫描所有在线账号的 Messenger 未读消息
- 检测到未读 → AI 生成回复 → 模拟人类打字发送
- 含**一级介绍检测**：当对方消息触发跨境电商业关键词时，AI 会做一级介绍并标记为重要记忆
- 回复后自动返回首页，保持干净状态

---

## 10. 运营策略配置

在「运营策略」页配置全局参数，存储在 `data/policy.json`。

### 10.1 可配置项

| 参数 | UI 字段 | 字段名 | 默认值 | 说明 |
|------|---------|--------|--------|------|
| 加好友上限 | `pfInvite` | `dailyInviteLimit` | 20 | 每账号每天最多好友邀请数 |
| 點讚上限 | `pfLike` | `dailyLikeLimit` | 50 | 每账号每天最多点赞数 |
| 分享上限 | `pfShare` | `dailyShareLimit` | 15 | 每账号每天最多分享贴文数 |
| 私信上限 | `pfMsg` | `dailyMsgLimit` | 20 | 每账号每天最多发消息数 |
| 最小延迟(秒) | `pfMin` | `minDelaySec` | 2 | 操作间最小随机延迟 |
| 最大延迟(秒) | `pfMax` | `maxDelaySec` | 8 | 操作间最大随机延迟 |
| 去重邀请 | `pfDedupInvite` | `dedupInvite` | true | 同一对象不重复邀请 |
| 去重分享 | `pfDedupShare` | `dedupShare` | true | 同一内容不重复分享 |

> 注：UI 静态默认值与后端 `DEFAULT_POLICY` 可能不同（如加好友界面初值 30、延迟 5-30 秒），以加载后的实际值/保存值为准。

### 10.2 策略与安全限制的关系

- **运营策略**（`policy.json`）：全局每日上限，适用于所有账号
- **安全限制**（内置，按阶段）：每账号每日上限，按 `stage` 分级

两者取较小值生效。即：如果策略设每日邀请 20，但新号阶段限制 5，则新号实际只能加 5 个。

---

## 11. 安全限制与防风控

### 11.1 三级安全限制

系统按账号阶段自动限制每日操作量，防止触发风控：

| 操作 | new（新号） | warmup（养号） | mature（成熟） |
|------|-------------|----------------|----------------|
| 加好友/天 | 5 | 10 | 20 |
| 加好友/小时 | 2 | 3 | 5 |
| 发消息/天 | 5 | 15 | 20 |
| 发贴文/天 | 1 | 3 | 5 |
| 点赞/天 | 10 | 20 | 40 |
| 留言/天 | 3 | 8 | 15 |
| 加社团/天 | 1 | 2 | 5 |

### 11.2 生涯社团上限

每个账号**一生最多加入 40 个社团**（`MAX_GROUPS_PER_ACCOUNT`，可在 `.env` 中修改）。已加入的社团 URL 会记录在 `joinedGroups` 字段中，去重计数。

### 11.3 拟人化行为

所有操作都模拟真人：

- **随机延迟**：操作间随机等待 2-8 秒（可配置），浏览贴文 10-60 秒
- **非线性滚动**：不是匀速滚到底，而是分段滚动 + 停顿
- **概率性点赞**：不是看到就点，30-60% 概率
- **人类打字**：模拟人类打字速度（逐字符输入 + 随机停顿）
- **社交错开**：互加好友错开 20-60 秒，互聊错开 15-45 秒，问候错开 5-30 分钟

### 11.4 风控状态自动处理

任务执行前后会自动检测页面状态：

| 检测到 | 处理 |
|--------|------|
| 身份验证/照片验证/checkpoint | 标记 `checkpoint`，停止任务，等待人工 |
| 账号禁用/锁定/封号 | 标记 `dead`，永久移出自动化 |
| 操作被阻止/频率警告 | 按 Escape，让任务自然失败 |
| Messenger PIN 弹窗 | 自动输入/建立 PIN（000000） |
| Cookie 同意弹窗 | 自动点击「允许所有 Cookie」 |
| 通知提示 | 自动点击「稍后再说」 |

---

## 12. 功能组合指南（典型工作流）

### 工作流 1：新账号冷启动

```
① 准备 xlsx（UID + Email + Cookie + 密码）
② 顶栏「📎 上傳檔案」选择 xlsx → 系统自动生成指纹 + 还原 Cookie + 分配代理
③ 进入「代理池」确认代理已绑定 + 点「⚡ 批量檢測」确认可用
④ 逐个点「▶ 啟動」账号 → 首登自愈管线自动执行：
     登录 → 设繁中 → 换头像 → 建 PIN → 点赞种子页
⑤ 确认所有账号状态为 idle
⑥ 第一周：每天只做 📰 瀏覽動態 + 👍 按讚貼文（new 阶段最保守）
⑦ 7 天后把 stage 改为 warmup
```

### 工作流 2：日常养号

```
① 启动要养号的账号（点「▶ 啟動」）
② 进入「任务编排」，在 FB 功能庫点选芯片
③ 依次执行：
     📰 瀏覽動態（browse_feed）
     👍 按讚貼文（like_post）
     🔍 搜尋加好友（search_add_friend，count=2-3）⚠️ 该芯片与后端名不一致，见第 7 节
④ 或直接点顶栏「🤖 AI 執行」让 AI 自动安排
⑤ 执行完毕后在「历史报告」确认结果
```

### 工作流 3：社交扩张

```
① 确保多个账号已登录
② 配置「社团数据源」：添加目标社团网址
③ 执行 add_friends_from_group（从社团加好友）⚠️ 仅 API：POST /api/task/run {"type":"add_friends_from_group"}
④ 执行 join_groups（加入新社团，界面芯片 🏛️ 加入社團）
⑤ 次日执行 greet_new_friends（问候昨天通过的好友，界面芯片 👋 問候新好友）
⑥ 执行 socialize（账号间互加互聊，界面芯片 🤝 互加互聊）
```

### 工作流 4：内容分发

```
① 准备内容：在内容库写一篇贴文（或用 AI 生成）
② 进入「任务编排」，勾选多个账号
③ 选择 📡 內容分發（content_dispatch，后端 distribute_content）⚠️ 名不一致见第 7 节
④ 系统为每个账号 AI 改写内容后错峰发帖
   （避免完全相同内容被判定垃圾信息）
```

### 工作流 5：AI 全自动运营

```
① 启动所有要运营的账号，确认已登录
② 确认已配置 DeepSeek API Key
③ 点击顶栏「🤖 AI 執行」（或账号卡片「🤖 智能執行」单账号执行）
④ AI 读取每个账号的记忆和阶段，差异化规划动作
⑤ 系统自动执行 AI 的计划
⑥ 开启被动聊天接管（/api/passive/start）自动回复未读消息
```

### 工作流 6：被动聊天接管

```
① 启动账号，确认已登录
② 调用 /api/passive/start（默认每 90 秒扫描一次）
③ 系统自动：
     扫描 Messenger 未读 → AI 生成回复 → 模拟打字发送
     检测一级介绍触发词 → 做跨境电商介绍 → 标记重要记忆
     回完返回首页保持干净
④ 在「全域监控」或「記憶體」查看聊天记录和接管事件
```

### 工作流 7：主页推广

```
① 在「社团数据源」添加你的公共主页网址
② 启动账号
③ 执行 👍 邀請讚主頁（invite_like_page，后端 invite_to_page）⚠️ 名不一致见第 7 节
④ 执行 auto_like_own_page（自动点赞自己主页的帖子）⚠️ 仅 API
⑤ 执行 share_post（分享主页贴文到时间线，界面芯片 📤 分享貼文）
⑥ 配合 📡 內容分發 多账号扩散
```

### 工作流 8：Cookie 失效修复

```
① 账号状态变为 needs_login 或 error
② 在「账号环境」找到该账号（界面无编辑按钮）
③ 通过 API 修改账号：POST /api/account/{id}/update
   重新填入 Cookie 字符串（或补充密码）
④ 保存（Cookie 同步写入浏览器档案）
⑤ 重新点「▶ 啟動」→ 系统用新 Cookie 直登
```

---

## 13. 数据目录结构

安装目录下 `data/` 文件夹存放所有用户数据：

```
data/
├── accounts.json              # 账号列表（含 UID/邮箱/Cookie/密码/状态/代理等）
├── proxies.json               # 代理池 + 分配表
├── policy.json                # 运营策略配置
├── openclaw-config.json       # AI 模型设置（baseUrl/apiKey/model）
├── skills.json                # 技能启用状态 + 使用统计
├── browser-profiles/          # 每个账号的浏览器档案（指纹/Cookie/缓存）
│   └── {accountId}/
├── screenshots/               # 截图（手动截图 + 错误截图）
├── logs/                      # 运行日志
├── content-library/           # 内容库（贴文草稿）
│   └── {postId}.json
├── avatars/
│   ├── inbox/                 # 待用头像
│   ├── used/                  # 已用头像
│   └── used-manifest.json     # 头像使用记录
└── (SQLite 记忆体文件)         # 每个账号的交互记忆
```

**备份**：复制整个 `data/` 目录即可完整备份所有数据。

**配置文件 `.env`**（可选，放在安装根目录）：

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `MOCK_FB` | 1 | 是否启用本地模拟 FB（1=启用，0=真实 FB） |
| `API_PORT` | 18991 | 服务端端口 |
| `MOCK_FB_PORT` | 18992 | Mock Facebook 端口（本地模拟 FB 服务） |
| `HEADLESS` | 0 | 是否无头模式（1=无头，0=有界面） |
| `OWN_PAGES` | （空） | 自动点赞的主页名称，逗号分隔 |
| `ONBOARDING_SEED_LIKE_URL` | （空） | 首登种子粉丝页 URL |
| `MAX_GROUPS_PER_ACCOUNT` | 40 | 每账号生涯社团上限 |

---

## 14. 常见问题与排错

### Q1：AI 对话没有回应

**原因**：未配置 DeepSeek API Key，或 Key 失效。

**解决**：
1. 进入「伺服器模型」页，确认已填写 apiKey 并保存（点「💾 保存並熱生效」）
2. 如果已保存但仍无回应，点击「🔧 執行修復」
3. 确认网关状态为「可达」

### Q2：点击「🤖 AI 執行」提示「请先配置模型 API」

**原因**：`data/openclaw-config.json` 中 `apiKey` 为空。

**解决**：在「伺服器模型」页填写并保存 DeepSeek API Key（点「💾 保存並熱生效」）。

### Q3：点击「🤖 AI 執行」提示「当前没有已登录的账号」

**原因**：AI 智能执行只针对有活跃浏览器会话（已启动并登录）的账号。

**解决**：先在「账号环境」页点「▶ 啟動」启动账号并确认登录成功（状态为 idle）。

### Q4：账号启动后状态变为 error

**可能原因**：
- 代理不可用或未绑定
- Cookie 失效且无密码
- 代理连接超时

**解决**：
1. 在「代理池」页检测该账号绑定的代理是否可用
2. 如果代理正常，检查账号是否有有效 Cookie 或密码
3. 重新导入 Cookie 或补充密码后重试

### Q5：账号状态变为 checkpoint

**原因**：触发了 Facebook 的安全验证（身份验证、照片验证等）。

**解决**：
1. 手动打开该账号的浏览器窗口
2. 完成 Facebook 要求的验证步骤
3. 验证通过后重新启动账号

### Q6：代理检测显示「不可用」但端口是通的

**原因**：本软件使用**真实穿透检测**（通过代理实际发 HTTPS 请求验证出口 IP），不是简单的端口检测。端口通但代理拒绝隧道（如 DataImpulse 未加白名单 IP）会被正确判定为不可用。

**解决**：
- DataImpulse 代理：在 DataImpulse 后台将你的本机 IP 加入白名单
- 确认用户名/密码正确
- 确认代理类型（DataImpulse 自动识别为 socks5）

### Q7：升级后数据会丢失吗

**不会**。直接覆盖安装不会影响 `data/` 目录。但建议升级前备份 `data/` 以防万一。

### Q8：如何切换账号阶段

在 `data/accounts.json` 中找到对应账号，把 `stage` 字段从 `new` 改为 `warmup` 或 `mature`，保存后重启服务端。或通过 API `POST /api/account/{id}/update` 修改（界面无编辑按钮）。

### Q9：如何让 AI 不调用某个技能

在「技能中心」页找到该技能，关闭启用开关。停用的技能不会被 AI 智能执行和 AI 对话调用。

### Q10：浏览器打不开

**原因**：自带 Chromium 可能被杀毒软件拦截，或权限不足。

**解决**：
1. 把安装目录加入杀毒软件白名单
2. 以管理员身份运行
3. 确认 `browser/chromium-1234/chrome-win64/chrome.exe` 存在

---

## 附录：API 接口速查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/status` | 服务端整体状态 |
| GET | `/api/accounts` | 账号列表 |
| POST | `/api/accounts` | 新增账号 |
| DELETE | `/api/accounts/{id}` | 删除账号 |
| POST | `/api/accounts/import-xlsx` | 批量导入 xlsx |
| GET | `/api/account/{id}` | 单账号详情 |
| POST | `/api/account/{id}/launch` | 启动账号 |
| POST | `/api/account/{id}/close` | 关闭账号 |
| POST | `/api/account/{id}/sync` | 同步状态 |
| POST | `/api/account/{id}/screenshot` | 截图 |
| POST | `/api/account/{id}/update` | 修改账号 |
| GET | `/api/account/{id}/state` | 会话状态 |
| POST | `/api/task/run` | 执行任务 |
| POST | `/api/chat` | AI 对话 |
| POST | `/api/agent/smart-execute` | AI 智能执行 |
| POST | `/api/agent/raw` | 直连 OpenClaw |
| GET | `/api/agent/health` | AI 健康状态 |
| POST | `/api/agent/supervise` | 运营监管 |
| GET | `/api/agent/memory` | 进化记忆 |
| GET | `/api/proxy` | 代理列表 |
| POST | `/api/proxy` | 添加代理 |
| POST | `/api/proxy/import` | 批量导入代理 |
| POST | `/api/proxy/test-all` | 全部检测 |
| POST | `/api/proxy/test/{id}` | 单条检测 |
| POST | `/api/proxy/{id}/assign` | 分配代理 |
| POST | `/api/proxy/{id}/unbind` | 解绑代理 |
| DELETE | `/api/proxy/{id}` | 删除代理 |
| POST | `/api/proxy/auto-assign` | 自动分配 |
| GET | `/api/sources` | 数据源列表 |
| POST | `/api/sources` | 添加数据源 |
| DELETE | `/api/sources/{id}` | 删除数据源 |
| GET | `/api/policy` | 读取策略 |
| POST | `/api/policy` | 保存策略 |
| GET | `/api/skills` | 技能列表 |
| POST | `/api/skills/{id}/toggle` | 启用/停用技能 |
| GET | `/api/memory/shards` | 记忆碎片 |
| GET | `/api/memory/shard/{id}` | 单账号记忆 |
| GET | `/api/monitor/state` | 监控快照 |
| GET | `/api/history` | 历史报告 |
| GET | `/api/logs` | 实时日志 |
| GET | `/api/settings/openclaw` | 读取模型设置 |
| POST | `/api/settings/openclaw` | 保存模型设置 |
| POST | `/api/openclaw/repair` | 一键修复 |
| GET | `/api/groups/joined` | 社团总览 |
| GET | `/api/avatar/stats` | 头像统计 |
| POST | `/api/avatar/mark-used` | 标记头像已用 |
| POST | `/api/avatar/apply` | 手动套用头像 |
| POST | `/api/warmup/run` | 养号周期 |
| POST | `/api/warmup/stop` | 停止养号 |
| POST | `/api/windows/retile` | 窗口重排 |
| POST | `/api/windows/auto-retile` | 自动重排开关 |
| POST | `/api/passive/start` | 启动被动接管 |
| POST | `/api/passive/stop` | 停止被动接管 |
| GET | `/api/perception` | 实时感知 |

---

*本手册基于 Aike-FBclaw v1.0.7 源代码编写，所有字段、格式、按钮名称、参数均逐一与源码核对。*
